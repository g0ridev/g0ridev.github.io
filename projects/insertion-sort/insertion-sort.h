#ifndef INSERTION_SORT_H
#define INSERTION_SORT_H

#include <vector>

// Template version (can sort any comparable type)
template <typename T>
void insertionSort(std::vector<T>& arr);

#endif // INSERTION_SORT_H
